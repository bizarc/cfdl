# Benchmark case: Damodaran's FCFF Simple Ginzu — textbook intrinsic valuation.
#
# THE FIRST OPCO CASE BUILT FROM PACK CONTRACTS. The other opco case reconciles
# a banker's DCF, but that filing publishes the RESULT — per-year unlevered cash
# flow — so it had to hand-write six native streams and validated the engine
# rather than the pack. This source publishes the DRIVERS, which is what a pack
# rule consumes. It takes opco off 0-of-10 contract types externally validated.
#
# License is explicit: "These spreadsheet programs are in Excel and are not copy
# protected. Download them and feel free to modify them to your own
# specifications." So the workbook is committed under reference/, as with HUD —
# a reader can open it and check every figure here directly.
#
# WHAT IS ASSERTED.
#
#   revenue / opex / taxes    years 1-10   exact
#   reinvestment              years 1-10   exact
#   model.npv                 the PV of the ten years of FCFF along the
#                             converging cost of capital, cell B20
#
# The growth and tax curves carry a PER-PERIOD rate. The rules compound it
# through a declared state (docs/14_state_and_recurrence.md), so the factor is
# the running product of each period's rate rather than pow(1 + g, t).
#
# Reinvestment is a DERIVED line — revenue(t) * g(t+1) / sales_to_capital —
# and opco.reinvestment states it that way: the model reads next year's growth
# with edate and the rule applies it to this year's modeled revenue.
#
# NOT ASSERTED: enterprise value and the per-share price, which need the
# terminal value and the balance-sheet bridge besides. See NOTES.md.
#
# period_tolerance = 0.001 — the published figures carry four decimals and the
# engine agrees to 1e-6 everywhere it is asserted.
summary = "A free cash flow to firm valuation following Damodaran's published method, with reinvestment driven by growth and return on capital."
pack = "opco"
period_tolerance = 0.001
