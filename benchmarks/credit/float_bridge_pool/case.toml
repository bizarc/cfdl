# Benchmark case: $15mm floating IO bridge pool, SOFR + 275 off a stepped
# forward curve with a binding 7.00% floor, 36mo bullet, 10 CPR, 2.5 CDR,
# 45% severity, 5mo recovery lag, purchased at par.
# Exercises the `curve` concept end-to-end (curve statement -> IR ->
# curve_value lookup with step interpolation).
# Reference: reference_gen.py (independent implementation).
# Status: pending practitioner Excel review.
pack = "credit"
period_tolerance = 0.01
