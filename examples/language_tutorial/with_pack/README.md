# With Pack

This example shows a slightly larger pack-enabled model.

Compile:

```bash
./target/debug/cfdl compile examples/language_tutorial/with_pack --out /tmp/tutorial_with_pack.ir.json --packs packs
```
